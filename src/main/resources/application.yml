spring:
  application:
    name: license-reclamation-engine
  
  datasource:
    url: jdbc:mysql://localhost:3306/license_engine?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  batch:
    job:
      enabled: false
  
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: akshusrathe007@gmail.com
    password: bski mcsq jlub mtfg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

server:
  port: 8082
  servlet:
    context-path: /

logging:
  level:
    com.company.licenseengine: DEBUG
    org.springframework.batch: INFO
    org.springframework.security: INFO

# Application specific properties
app:
  mock-apis:
    hris-url: http://localhost:8082/mock/hris
    vendor-url: http://localhost:8082/mock/vendor
  email:
    verification-base-url: https://wheaten-nondenunciating-george.ngrok-free.dev
    response-deadline-days: 0  # Changed to 0 days for testing
    response-deadline-minutes: 4  # Added 4 minutes for testing
    reminder-before-minutes: 2  # Send reminder 2 minutes before deadline
    extension-duration-days: 0  # Changed to 0 days for testing
    extension-duration-minutes: 4  # Extension lasts 4 minutes for testing
  scheduling:
    deadline-check-cron: "0 * * * * *" # Every minute for testing
    extension-check-cron: "30 * * * * *" # Every minute at 30 seconds for testing